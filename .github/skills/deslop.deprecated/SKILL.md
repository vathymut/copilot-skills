---
name: deslop
description: DEPRECATED — merged into code-review (Maintainability axis). Kept as a user-invoked alias that forwards to code-review.
---

# Deprecated

`deslop` has been merged into `code-review` § Maintainability axis, which now
explicitly covers AI-generated slop removal alongside structural review.

If you arrived here manually, invoke `code-review` instead.
